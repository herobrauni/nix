# Nix Repo — Improvement Suggestions

Reviewed all modules, flake config, CI, and PLAN.md. Organized by priority.

---

## HIGH PRIORITY

### 1. ~~Impermanence is missing critical persistent directories~~ ✅ DONE
Added `/var/lib/acme`, `/etc/adjtime` to system persistence. Added full `homeManager` block with home persistence (`.ssh`, `.config`, `.local/share`, `.cache`, `.bash_history`).

### 2. ~~Home persistence is commented out~~ ✅ DONE
Moved home persistence into the `impermanence.nix` aspect's `homeManager` block, so it's only active on hosts that include impermanence. Removed the TODO from `brauni.nix`.

### 3. ~~PLAN.md is stale and has inaccuracies~~ ✅ DONE
Fixed file tree to match actual layout, removed completed TODOs, updated paths and descriptions.

### 4. ~~No flake checks or linting~~ ✅ DONE
Added `modules/checks.nix` with `nixfmt`, `statix`, and `deadnix` checks. Run with `nix flake check`.

### 5. ~~Security hardening in base-server~~ ✅ DONE
Added `X11Forwarding = false`, `MaxAuthTries = 3`, `ClientAliveInterval = 300`, `ClientAliveCountMax = 2`.

---

## MEDIUM PRIORITY

### 6. ~~agenix input doesn't follow nixpkgs~~ ✅ DONE
Added `inputs.nixpkgs.follows = "nixpkgs"` to agenix in `dendritic.nix`.

### 7. ~~nixtest2 uses legacy networking config~~ ✅ DONE
Converted to `systemd.network` style matching nixtest1.

### 8. ~~VM module only builds nixtest1~~ ✅ DONE
Now accepts a host argument: `nix run .#vm -- nixtest2`. Defaults to `nixtest1`.

### 9. ~~No disko for nixtest2~~ ✅ DONE
Added explanatory comment in `nixtest2.nix` about intentional lack of disko.

### 10. ~~Duplicate packages between base-server and user~~ ✅ DONE
Removed `jq`, `ripgrep` from system packages (kept in home). Removed `curl`, `wget` from nixtest1 host config.

### 11. ~~Git config is minimal~~ ✅ DONE
Added `userName = "brauni"`, `init.defaultBranch = "main"`, `core.autocrlf = "input"`. Email left out intentionally (add via agenix or when ready).

---

## LOW PRIORITY / NICE-TO-HAVE

### 12. ~~Add a README.md~~ ✅ DONE

### 13. Consider treefmt over manual nixfmt
[treefmt](https://github.com/numtide/treefmt) gives you one config for all formatters. Could replace the manual checks.nix approach later.

### 14. nixpkgs input uses tarball URL
`nixpkgs.url = "https://channels.nixos.org/nixpkgs-unstable/nixexprs.tar.xz"` — the GitHub form (`github:NixOS/nixpkgs/nixpkgs-unstable`) is faster to fetch. This is auto-generated by `flake-file` so would need to be fixed upstream or in dendritic.nix.

### 15. Add nixos-anywhere as a flake package
Consider adding it as a flake input or devShell package for `nix run .#deploy-nixtest1`.

### 16. Consider sops-nix as alternative to agenix
Not urgent. sops-nix supports age + GPG + KMS with better multi-key rotation.

### 17. Auto-upgrade references `github:herobrauni/nix`
Every host needs GitHub access on upgrade. Consider a private Git server for internal-only hosts.

### 18. ~~Missing .editorconfig~~ ✅ DONE

### 19. ~~Move stateVersion to per-host config~~ ✅ DONE
Removed from `defaults.nix`, added `system.stateVersion = "25.11"` to each host file.

### 20. ~~CI could use DeterminateSystems actions~~ ✅ DONE
Switched from `cachix/install-nix-action` to `DeterminateSystems/nix-installer-action` + `magic-nix-cache-action`. Pinned action versions to `@v4` / `@v16` / `@v9`.

---

## SUMMARY

| Priority | Total | Done | Remaining |
|----------|-------|------|-----------|
| High     | 5     | 5    | 0         |
| Medium   | 6     | 6    | 0         |
| Low      | 9     | 4    | 5         |

All high and medium items resolved. Remaining low items are nice-to-haves (treefmt, nixpkgs URL, nixos-anywhere package, sops-nix consideration, auto-upgrade URL).
